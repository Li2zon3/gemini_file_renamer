# Gemini File Renamer Dependencies
# 运行 pip install -r requirements.txt

# Core dependencies / 核心依赖
google-generativeai>=0.3.0
pymupdf>=1.23.0
pathvalidate>=3.0.0
python-docx>=1.0.0
EbookLib>=0.18
beautifulsoup4>=4.12.0

# Build tools (for packaging) / 构建工具（用于打包）
pyinstaller>=6.0.0
